[build]
  command = "npm run build --prefix frontend"
  publish = "frontend/build"

[functions]
  directory = "backend"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200